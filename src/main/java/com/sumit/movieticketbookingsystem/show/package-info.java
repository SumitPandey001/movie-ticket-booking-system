/**
 * Shows: scheduling, the browse pages and the seat map.
 */
@ApplicationModule(allowedDependencies = {"catalog", "inventory", "pricing", "shared"})
package com.sumit.movieticketbookingsystem.show;

import org.springframework.modulith.ApplicationModule;
