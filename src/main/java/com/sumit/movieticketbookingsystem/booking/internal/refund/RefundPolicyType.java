package com.sumit.movieticketbookingsystem.booking.internal.refund;

public enum RefundPolicyType {
    /** A percentage that depends on how long before the show the customer cancels. */
    SLAB,
    FULL,
    NON_REFUNDABLE
}
