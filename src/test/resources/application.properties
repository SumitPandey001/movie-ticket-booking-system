# Loaded next to src/main/resources/application.yml (different extension, so it doesn't replace it).
# Scheduled jobs would fire in the middle of tests that move the clock; tests call runOnce() instead.
jobs.enabled=false
